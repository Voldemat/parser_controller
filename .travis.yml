language: python

python: 
        - 3.8
before_install:
        - python --version
        - pip install --upgrade pip
install:
        - pip install -r requirements.txt
script:
        - python -m unittest parser_controller
